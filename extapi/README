This directory holds files that are external to NCAm.

These files are API definitions or libs that are better to be shipped
with NCAM than to depend on founding them on the system that NCAm is
being compiled on.
